package seedu.addressbook.data.person;

/**
 * A printable interface for data of a person in the addressbook
 * Implementations should be able to print details of the data
 */

public interface Printable {
    String getPrintableString();
}
